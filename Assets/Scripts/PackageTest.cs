using System.Collections;
using System.Collections.Generic;
using Top2.Common.Http;
using UnityEngine;

public class PackageTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        HttpTools.PrintTest();
    }

    // Update is called once per frame
    void Update()
    {
    }
}